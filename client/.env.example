# Backend URL when it's not on the same origin as the client
# (e.g. testing from another device on the LAN). Leave unset for
# localhost dev (auto-detected) or same-origin production builds.
VITE_API_URL=
